export const fontSizes = {
  xs: 10, // extra small (e.g., captions)
  sm: 12, // small (e.g., comment text)
  md: 14, // medium (e.g., post content)
  lg: 16, // large (e.g., usernames)
  xl: 18, // extra large (e.g., buttons, bold text)
  xxl: 22, // big (e.g., section headers)
  title: 28, // titles or large headers
};

export const fontWeights = {
  thin: 100,
  extraLight: 200,
  light: 300,
  normal: 400,
  medium: 500,
  semiBold: 600,
  bold: 700,
  extraBold: 800,
  black: 900,
};
